'use client';

import React from 'react'
const AddToCart = () => {
  return (
    <div>
      <button className='btn btn-primary w-72 mt-3' onClick={() => alert('Add Product to cart?') }>Add to cart</button>
    </div>
  )
}

export default AddToCart
